<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.tadoya.din9talk" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".StartActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChattingActivity"
            android:label="@string/label_chatting"/>

        <activity
            android:name="com.google.firebase.quickstart.auth.CustomAuthActivity"
            android:label="@string/label_custom_auth" />

        <activity
            android:name="com.google.firebase.quickstart.auth.GoogleSignInActivity"
            android:label="@string/label_google_sign_in" />

        <activity
            android:name="com.google.firebase.quickstart.auth.FacebookLoginActivity"
            android:label="@string/label_facebook_login" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <activity
            android:name="com.google.firebase.quickstart.auth.EmailPasswordActivity"
            android:label="@string/label_emailpassword"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <activity
            android:name="com.google.firebase.quickstart.auth.AnonymousAuthActivity"
            android:label="@string/label_anonymous_auth"/>

        <activity
            android:name="com.google.firebase.quickstart.auth.ChooserActivity"
            android:label="@string/Chooser"/>

        <!-- [START firebase_service] -->
        <service android:name="com.google.firebase.quickstart.fcm.MyFirebaseMessagingService" >
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- [END firebase_service] -->
        <!-- [START firebase_iid_service] -->
        <service android:name="com.google.firebase.quickstart.fcm.MyFirebaseInstanceIDService" >
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <!-- [END firebase_iid_service] -->
    </application>

</manifest>